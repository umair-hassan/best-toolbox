best_toolbox.m protocol = the toolbox version that is used when launching BEST in matlab.

Backup folder contains best_toolbox_nsl1_synched.m & best_toolbox_nsl3_orig.m versions. 

If unsure which version is currently being used, compare to the backup copies.